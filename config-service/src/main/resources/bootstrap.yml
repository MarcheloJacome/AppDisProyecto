server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MarcheloJacome/AppDisProyecto.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: Eureka-server
  security:
    user:
      name: root
      password: s3cr3t